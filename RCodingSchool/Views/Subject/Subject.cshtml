@model List<RCodingSchool.ViewModels.SubjectVM>
@{
    ViewBag.Title = "Subject";
}
<div class="container">
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>Назва предмету</th>
                    <th>Вид контролю</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var x in Model)
                {
                    <tr>
                        <td>@x.Name</td>
                        @if (x.IsExam)
                        {
                            <td>Екзамен</td>
                        }
                        else
                        {
                            <td>Залік</td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

